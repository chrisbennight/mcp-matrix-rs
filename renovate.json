{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "enabledManagers": ["cargo", "github-actions", "custom.regex"],
  "labels": ["dependencies"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Version pins declared under a `# renovate:` hint comment: the rustup-init installer in .github/workflows/test.yml and the toolchain channel in rust-toolchain.toml. The installer checksum is updated manually so a mismatched version fails CI loudly.",
      "fileMatch": ["^\\.github/workflows/test\\.yml$", "^rust-toolchain\\.toml$"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>[^\\s]+)\\s*\\n\\s*(?:[A-Z0-9_]+|channel)\\s*[:=]\\s*\"(?<currentValue>[^\"]+)\""
      ]
    }
  ]
}
